apiVersion: v1
kind: Service
metadata:
  name: {{ include "taka-auth.fullname" . }}
  labels:
    {{- include "taka-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      name: {{ .name }}
    {{- end }}
  selector:
    {{- include "taka-auth.selectorLabels" . | nindent 4 }}
