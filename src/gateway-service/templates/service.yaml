apiVersion: v1
kind: Service
metadata:
  name: {{ include "taka-gateway.fullname" . }}
  labels:
    {{- include "taka-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      name: {{ .name }}
    {{- end }}
  selector:
    {{- include "taka-gateway.selectorLabels" . | nindent 4 }}
