apiVersion: v1
kind: Service
metadata:
  name: {{ include "taka-masking.fullname" . }}
  labels:
    {{- include "taka-masking.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      name: {{ .name }}
    {{- end }}
  selector:
    {{- include "taka-masking.selectorLabels" . | nindent 4 }}
