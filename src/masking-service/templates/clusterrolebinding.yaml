{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "taka-masking.fullname" . }}-{{ .Release.Namespace }}-clusterrolebinding
  labels:
    {{- include "taka-masking.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "taka-masking.fullname" . }}-{{ .Release.Namespace }}-clusterrole
subjects:
  - kind: ServiceAccount
    name: {{ include "taka-masking.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
