apiVersion: v1
kind: Service
metadata:
  name: {{ include "taka-crypto.fullname" . }}
  labels:
    {{- include "taka-crypto.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      name: {{ .name }}
    {{- end }}
  selector:
    {{- include "taka-crypto.selectorLabels" . | nindent 4 }}
