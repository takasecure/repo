{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "taka-crypto.fullname" . }}-{{ .Release.Namespace }}-clusterrolebinding
  labels:
    {{- include "taka-crypto.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "taka-crypto.fullname" . }}-{{ .Release.Namespace }}-clusterrole
subjects:
  - kind: ServiceAccount
    name: {{ include "taka-crypto.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
