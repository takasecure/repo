apiVersion: v1
kind: Service
metadata:
  name: {{ include "taka-dispatch.fullname" . }}
  labels:
    {{- include "taka-dispatch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      name: {{ .name }}
    {{- end }}
  selector:
    {{- include "taka-dispatch.selectorLabels" . | nindent 4 }}
