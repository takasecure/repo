apiVersion: v1
kind: Service
metadata:
  name: {{ include "taka-backup.fullname" . }}
  labels:
    {{- include "taka-backup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      name: {{ .name }}
    {{- end }}
  selector:
    {{- include "taka-backup.selectorLabels" . | nindent 4 }}
